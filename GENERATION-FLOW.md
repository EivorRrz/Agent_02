# 🔄 Artifact Generation Flow

## **Complete Flow: Upload → Process → Generate**

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER UPLOADS FILE                            │
│              POST /upload/ingest                                │
│           (Excel/CSV with metadata)                             │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                  MODULE 1: File Upload Handler                  │
│  - Validates file type (Excel/CSV)                              │
│  - Generates unique fileId                                      │
│  - Saves file to uploads/                                       │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│              MODULE 2: Excel/CSV Parser                         │
│  - Parses all sheets (Excel) or rows (CSV)                      │
│  - Extracts: tableName, columnName, dataType, etc.              │
│  - Handles BOM, special characters, missing columns             │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│            MODULE 3: PK/FK Heuristics                           │
│  - Infers Primary Keys (naming patterns, explicit flags)        │
│  - Infers Foreign Keys (references, patterns)                   │
│  - Adds _pkSource, _fkSource for traceability                   │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│            MODULE 4: LLM Assist (Optional)                      │
│  - Sends metadata to Ollama (DeepSeek-R1:7B)                    │
│  - Gets AI-powered PK/FK suggestions                            │
│  - Merges with heuristics (AI overrides if confident)           │
│  - Adds confidence scores and rationales                        │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│              SAVE METADATA TO DISK                              │
│  artifacts/{fileId}/metadata.json                               │
│  - All parsed columns                                           │
│  - Inferred PK/FK relationships                                 │
│  - Table structure                                              │
│  - LLM status and results                                       │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │ Response: { fileId: "123456..." }
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│          USER TRIGGERS GENERATION                               │
│          POST /generate/{fileId}                                │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│              LOAD METADATA FROM DISK                            │
│  Read: artifacts/{fileId}/metadata.json                         │
└────────────────────┬────────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┬────────────┐
        │                         │            │
        ▼                         ▼            ▼
┌───────────────┐       ┌────────────────┐    │
│ Generate DBML │       │ Generate       │    │
│ (Logical)     │       │ Mermaid ERD    │    │
│               │       │                │    │
│ - Tables      │       │ - Entities     │    │
│ - Columns     │       │ - Attributes   │    │
│ - PK/FK       │       │ - Relationships│    │
│ - Constraints │       │                │    │
└───────┬───────┘       └────────┬───────┘    │
        │                        │            │
        ▼                        ▼            │
   schema.dbml                erd.mmd         │
        │                        │            │
        │                        │            │
        │                        ├────────────┘
        │                        │
        │                        ▼
        │              ┌──────────────────┐
        │              │   Puppeteer      │
        │              │  (Headless       │
        │              │   Chrome)        │
        │              │                  │
        │              │  Renders Mermaid │
        │              │  to Images       │
        │              └────┬─────┬───┬───┘
        │                   │     │   │
        │                   ▼     ▼   ▼
        │              erd.png  .svg  .pdf
        │                   │     │   │
        └───────────────────┴─────┴───┴──────────┐
                                                  │
                                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│              ALL ARTIFACTS SAVED                                │
│  artifacts/{fileId}/                                            │
│  ├── metadata.json      ✅                                      │
│  ├── schema.dbml        ✅ (Logical Model)                      │
│  ├── erd.mmd            ✅ (Mermaid Source)                     │
│  ├── erd.png            ✅ (Picture)                            │
│  ├── erd.svg            ✅ (Vector)                             │
│  └── erd.pdf            ✅ (Document)                           │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│              RETURN RESULTS TO USER                             │
│  {                                                              │
│    status: "success",                                           │
│    artifacts: {                                                 │
│      dbml: { path: "...", size: 12345 },                        │
│      mermaid: { path: "...", size: 8901 },                      │
│      images: { svg: "...", png: "...", pdf: "..." }             │
│    }                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## **Key Components**

### **Input**
- Excel/CSV file with metadata
- API Key for security

### **Processing**
1. Parse file → Extract metadata
2. Infer PK/FK → Heuristics + LLM
3. Save metadata → File system

### **Generation**
1. DBML → Logical model (text)
2. Mermaid → ERD syntax (text)
3. Puppeteer → Render images (PNG/SVG/PDF)

### **Output**
- `schema.dbml` - Logical model
- `erd.png` - Picture
- `erd.svg` - Vector graphic
- `erd.pdf` - Printable document

---

## **API Endpoints**

| Endpoint | Method | Purpose |
|----------|--------|---------|
| `/upload/ingest` | POST | Upload & process file |
| `/generate/:fileId` | POST | Generate artifacts |
| `/generate/:fileId/status` | GET | Check artifact status |
| `/health` | GET | Server health check |

---

## **Files Modified/Created**

### **Created** ✨
- `src/generators/erdGenerator.js` - ERD generation logic
- `src/generators/index.js` - Export all generators
- `src/routes/generate.js` - Generation API endpoints
- `MODULE-5-COMPLETE.md` - Documentation
- `GENERATION-FLOW.md` - This file

### **Modified** 🔧
- `src/server.js` - Added generate route
- `src/generators/dbmlGenerator.js` - Fixed minor issues

---

## **Dependencies**

```json
{
  "puppeteer": "^24.35.0",     // ERD image generation
  "@dbml/core": "^5.4.0",       // Future: DBML parsing
  "express": "^4.18.2",         // Web server
  "multer": "^2.0.0",           // File uploads
  "xlsx": "0.20.1",             // Excel parsing
  "csv-parse": "^5.5.2",        // CSV parsing
  "pino": "^8.16.2"             // Logging
}
```

---

## **Performance Metrics**

| Operation | Time | Notes |
|-----------|------|-------|
| File Upload | ~100ms | Depends on file size |
| Parsing | ~200-500ms | Excel slower than CSV |
| Heuristics | ~50-100ms | Fast, rule-based |
| LLM Assist | ~2-5s | Depends on model size |
| DBML Generation | ~100ms | Text generation |
| Mermaid Generation | ~50ms | Text generation |
| **Puppeteer Render** | **~3-8s** | **Slowest part** |
| **Total** | **~5-15s** | Varies by complexity |

---

**Ready for Production Testing!** 🚀

